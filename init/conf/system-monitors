# This configuration file contains the default parameters of the Naro Nanins
# system monitors startup script. Make sure to restart the corresponding
# service for changed parameters to take effect.

# Set RUN=yes to start the system monitors at boot time.
RUN=no

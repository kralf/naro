# This configuration file contains the default parameters of the Naro Nanins
# USB servo controller server startup script. Make sure to restart the
# corresponding service for changed parameters to take effect.

# Set RUN=yes to start the USC server at boot time.
RUN=yes

# This configuration file contains the default parameters of the Naro Nanins
# depth sensor startup script. Make sure to restart the corresponding service
# for changed parameters to take effect.

# Set RUN=yes to start the depth sensor at boot time.
RUN=yes

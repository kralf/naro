# This configuration file contains the default parameters of the Naro Nanins
# joystick command startup script. Make sure to restart the corresponding
# service for changed parameters to take effect.

# Set RUN=yes to start the joystick command at boot time.
RUN=yes

# This configuration file contains the default parameters of the Naro Nanins
# joystick control startup script. Make sure to restart the corresponding
# service for changed parameters to take effect.

# Set RUN=yes to start the joystick control at boot time.
RUN=no

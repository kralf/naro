% SP Jonas

filePath = fullfile('../bagFiles', '2015-05-29-15-33-14.bag');
bag = rosbag(filePath)

pitchLog = select(bag, 'Topic', '/naro_monitoring/pitchLog');
msgs = readMessages(pitchLog);
n = length(msgs);
m = length(msgs{1}.Data)
pitchLogData = zeros(n)
